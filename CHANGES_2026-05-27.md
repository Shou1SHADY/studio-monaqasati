# Changes Log — 2026-05-27

## 1. Infrastructure

| File | Change |
|------|--------|
| `.gitignore` | Added `.playwright-mcp/`, `*.ai-screenshot.png`, screenshot files |
| `messages/en.json` | Added hero checkmark keys, testimonial translations, forgot password keys |
| `messages/ar.json` | Added hero checkmark keys, testimonial translations, forgot password keys |

## 2. Locale Routing Fixes (15 files)

**Problem:** `useRouter`, `usePathname`, `Link`, `redirect` were imported from `next/navigation` instead of `@/i18n/routing`, causing navigation to always go to the default locale (Arabic) regardless of current language.

| File | Fix |
|------|-----|
| `src/components/layout/portal-layout.tsx` | Changed to locale-aware router/pathname, banner link to profile |
| `src/components/layout/role-sidebar.tsx` | Changed to locale-aware pathname, sidebar side+locale aware |
| `src/components/chat-page-content.tsx` | Locale-aware router |
| `src/components/chats-list-page.tsx` | Locale-aware router, Chevron direction, unread dot position |
| `src/components/supplier/SubmitOfferDialog.tsx` | Locale-aware router |
| `src/components/team-management.tsx` | Table overflow, dialog dir, search icon position |
| `src/components/ui/dialog.tsx` | Close button: `left-4` → `end-4` (logical CSS) |
| `src/components/ui/sheet.tsx` | Close button: `right-4` → `end-4` (logical CSS) |
| `src/app/[locale]/login/page.tsx` | Forgot password dialog |
| `src/app/[locale]/register/page.tsx` | Bilingual category display |
| `src/app/[locale]/verify-email/page.tsx` | Locale-aware router |
| `src/app/[locale]/chat/[chatId]/page.tsx` | Locale-aware router |
| `+ 8 page files` | Changed from `next/link` to `@/i18n/routing` Link |

## 3. RTL/LTR Layout Fixes (25 files)

**Problem:** Hardcoded `text-right`, `mr-auto`, `right-*`, `left-*` classes forced RTL layout in English mode.

| File | Fix |
|------|-----|
| `portal-layout.tsx` | Search icon, input padding, header alignment, close button position all locale-aware |
| `role-sidebar.tsx` | Side side + border flip via locale |
| `chats-list-page.tsx` | Unread dot position, time alignment |
| `contractor/page.tsx` | Gradient directions, ArrowRight rotation, PlusCircle margin, decorative blobs |
| `contractor/profile/page.tsx` | Verified badge position, action bar margin, gradient direction, section text-right |
| `contractor/suppliers/page.tsx` | All filter dropdowns bilingual, dialog dir, text alignment |
| `contractor/rfqs/page.tsx` | Filter dropdowns bilingual, search icon, checkbox margin |
| `contractor/rfqs/new/page.tsx` | City select bilingual, container text-right |
| `contractor/rfqs/[id]/offers/page.tsx` | Container text-right locale-aware, dialog dir |
| `supplier/page.tsx` | Container text-right locale-aware |
| `supplier/profile/page.tsx` | Dropdown dir, city display, spec display, container text-right |
| `supplier/rfqs/page.tsx` | Container text-right, filter widths + scroll, chevron direction |
| `supplier/offers/page.tsx` | Container text-right locale-aware |
| `supplier/orders/page.tsx` | Container text-right locale-aware |
| `app/[locale]/page.tsx` (landing) | Hero badge removed, all sections responsive, testimonials dir |
| `components/ui/dialog.tsx` | Close button: `left-4` → `end-4` |
| `components/ui/sheet.tsx` | Close button: `right-4` → `end-4` |

## 4. Profile Tour Guide (3 files)

**New feature:** Step-by-step tour that activates on first profile visit or via `?tour=true` param.

| File | Change |
|------|--------|
| `src/components/profile-tour.tsx` | **NEW** – Tour popup with animated overlay, step counter, position logic |
| `contractor/profile/page.tsx` | Integrated tour state, 8 steps (5 basic info + 3 legal docs), auto-tab switching |
| `portal-layout.tsx` | Banner button points to `profile?tour=true` |

## 5. Bilingual Dropdowns (7 files)

**Problem:** All category/city dropdowns showed Arabic-only names even in English mode.

| File | Fix |
|------|-----|
| `src/lib/constants.ts` | Added `SUBCATEGORIES_EN` (175+ subcategories), `displayCategory`, `displayCity`, `displaySubcategory` helpers |
| `contractor/rfqs/page.tsx` | Category + city dropdowns use `displayCategory` / `displayCity`, added `max-h-72 overflow-y-auto` |
| `contractor/rfqs/new/page.tsx` | City dropdown uses `displayCity` |
| `contractor/profile/page.tsx` | City dropdown uses `displayCity` |
| `supplier/rfqs/page.tsx` | Category + city dropdowns use helpers |
| `supplier/profile/page.tsx` | Specializations + cities bilingual, dropdown dir fixed |
| `register/page.tsx` | Specialization list uses `displayCategory` |

## 6. Forgot Password (1 file + messages)

**Problem:** The "Forgot Password?" link on the login page was a dead link (`href="#"`).

| File | Change |
|------|--------|
| `login/page.tsx` | Added dialog, `sendPasswordResetEmail` handler, success/error states |
| `messages/en.json` | Added 10 new translation keys |
| `messages/ar.json` | Added 10 new translation keys |

## 7. Landing Page Enhancements (1 file + messages)

| Area | Change |
|------|--------|
| Hero | Removed badge chip, added checkmark trust badges, secondary CTA, flex layout for heading |
| Stat cards | Changed `min-w-[260px]` horizontal scroll to proper 2-col grid on mobile |
| Features | Reduced padding on mobile (`p-10` → `p-6`) |
| Partner logos | Cleaner design, `bg-white/5` containers, seamless scroll animation, 4 copies |
| All sections | Reduced `py-32` → `py-16 md:py-24 lg:py-32` for mobile |
| Headings | Responsive sizing (`text-3xl` mobile → `text-7xl` desktop) |
| CTA | Redesigned: grid overlay, 3 glow orbs, floating pills, gradient title, animated pulse |
| Testimonials | **Added but commented out** — ready for when client testimonials are collected |
| Hero overflow | Removed `overflow-hidden` from hero section (was clipping text bottom) |
| Logo animation | Fixed to `-25%` translation with 4 copies for seamless loop |

## 8. Profile Verified Badge Fix

| File | Change |
|------|--------|
| `contractor/profile/page.tsx` | Added `isVerified` to profile state, synced from Firestore, conditional badge render |

## 9. Legal Document Banner Logic

| File | Change |
|------|--------|
| `portal-layout.tsx` | Banner now shows until CR + VAT legal documents are uploaded (`!legalDocuments?.cr?.url \|\| !legalDocuments?.vat?.url`) |

## 10. Brand Name Update

**Problem:** "Monaqasati" branding text still appeared in login, register, and verify-email pages.

| File | Change |
|------|--------|
| `login/page.tsx` | "Monaqasati" → "Mdmak Tech" (×2) |
| `register/page.tsx` | "Monaqasati" → "Mdmak Tech" (×2) |
| `verify-email/page.tsx` | "Monaqasati" → "Mdmak Tech" |

## 11. Dead Code & Cleanup

| File | Change |
|------|--------|
| `contractor/page.tsx` | Removed local `cn()` function, imported from `@/lib/utils` |
| `contractor/profile/page.tsx` | Fixed accidental removal of `profile` state in tour integration |
| `profile-tour.tsx` | Removed unused `prevStepRef` |
| `supplier/profile/page.tsx` | Removed duplicate `useEffect` import |
| `components/team-management.tsx` | Added `overflow-x-auto` to table container |
| Multiple files | Added missing `cn` imports in 4+ files |
